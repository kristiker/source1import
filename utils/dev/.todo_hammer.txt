add a static overlay and try to read its
...same as info_overlay
see how they get cut -- if easy, make a formula derived from info_overlay

ultimately: see if you can replace info_overlays with static overlays

MAPS:

light_environment:
    specular: render to cubemaps
